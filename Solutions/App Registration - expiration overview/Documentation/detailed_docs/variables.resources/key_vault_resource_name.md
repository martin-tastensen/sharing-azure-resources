# key_vault_resource_name
**File:** variables.resources.tf</br></br>

Provide name for key-vault, The key vault will be used to store secrets that we don't wish to store in clear text (max 24 characters)